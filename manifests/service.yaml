apiVersion: v1
kind: Service
metadata:
  name: fraud-detector
  namespace: default
spec:
  type: NodePort
  selector:
    app: fraud-detector
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
